import React from "react";

const Loading = () => {
  return (
    <section className="min-w-full min-h-full flex justify-center items-center">
      Loading...
    </section>
  );
};

export default Loading;
