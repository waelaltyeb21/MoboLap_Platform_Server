import { Cairo, Zain } from "next/font/google";
import "./globals.css";
import MainLayout from "@/layouts/MainLayout";
import { cookies } from "next/headers";

const zain = Zain({
  variable: "--font-zain",
  weight: ["200", "300", "400", "700", "800", "900"],
  subsets: ["latin"],
});

export const metadata = {
  title: "Mobolap Platform",
  description: "Generated by create next app",
};

export default async function RootLayout({ children }) {
  const AllCookies = await cookies();
  const mode = AllCookies.get("mode")?.value || "light";
  console.log("Loaded Mode: ", mode);
  return (
    <html lang="ar" dir="rtl" className={`${mode}`}>
      <body className={`${zain.variable} antialiased`}>
        <MainLayout>{children}</MainLayout>
      </body>
    </html>
  );
}
