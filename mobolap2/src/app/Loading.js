import React from "react";

const Loading = () => {
  return (
    <section className="w-full h-[30dvh] flex justify-center items-center">
      <h1 className="text-xl font-semibold">يتم التحميل...</h1>
    </section>
  );
};

export default Loading;
